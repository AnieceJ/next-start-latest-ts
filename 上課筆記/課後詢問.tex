-- 營運部署 (一般情況下用不到這個指令)--
npm run build
打包時會跳出ESlint錯誤

npm run start

RCCP & RSC

React跟Next.js實際上的關係?
在操作的內容是React還是Next.js?
還是跟Node.js一樣, Node.js是地基, prisma是工具這種關係?

ch3-jsx/render的內容詢問

5/28
React基礎章節:
JSX
State
Props
Form + ref
Life cycle(effect) + api

5/29
****************************
Image是Next優化圖片的自帶套件,
如果在map裡面直接加入Image會跳錯(Next不允許連外, 這裡示範檔是連結外部網路圖片的)
要改next.config.ts做設定(允許)

(next.config.ts)設定:
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      },
    ],
  },
};

export default nextConfig;
****************************

if流程控制的表達式語法 - 使用邏輯與&&
常用? 閱讀性? 造成混亂?

ch4-state/cart
問ai 要學到全部每一行都要會寫


Refactor(-ing) : 重構
何謂重構, 做到哪種地步才算重構?

MVP : 最小可行產品
何謂最小可行產品, 這是專案最基本的動作嗎?

interface練習(沒學到)

(cart-v1檔案page.tsx) 完整練習
setState? 老師說這邊最難理解(提到是否有執行順序?, 由React決定? console.log抓不到東西? 整理程式碼哪些是需要改變的, 再去做修改? setState其實是一起執行? 呼叫其他函式的時候要很小心?)
useState? 怎麼設定? 必定初始值? 空字串? 0?

函式閉包?到底什麼意思
理解setState什麼時候用閉包prev=> prev +1? 什麼時候用count+1 實務上哪個常用? (老師說count+1?)
閉包陷阱?

淺拷貝(展開運算子), 深拷貝?(for迴圈?)

-- todo list(ch4-state/todo/page.tsx) --
可看到useState('aaa')
在瀏覽器devtools > components > 搜尋todo > 找到ClientPageRoot()TodoPage > hook裡面State
看到input的value
為何input的value寫上{inputText}的時候就是由useState裡面的()內容做決定? useState裡面的()設定到底是什麼? 對應inputText?

老師說迭代再寫迭代會撞名, 到底什麽是迭代?

用React練習todo清單(按照步驟練習)
寫好html
設定狀態state
在html新增改變狀態的屬性
建立輸入todo時的函式
在html套用函式
新增刪除函式
在html新增button並套用刪除函式
(學會寫map跟filter, 步驟要放在哪? 已完成要用刪除還是改變狀態?)

immer是什麼, 跟一般JSX語法不一樣嗎?
